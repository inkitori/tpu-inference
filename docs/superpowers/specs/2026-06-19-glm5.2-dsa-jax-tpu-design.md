# GLM 5.2 (DSA) on JAX/TPU — Design Spec (TPU-forward)

**Date:** 2026-06-19
**Status:** Approved design. Ready for implementation planning (`writing-plans`).
**Supersedes:** an earlier spec that targeted the WRONG class — `Glm4MoeLiteForCausalLM` = GLM-4.7-Flash (MLA-only, no DSA); corrected in §1.1. (A CPU-first rework of this spec was also replaced by the TPU-first strategy below.) Both recoverable from git.
**Companion docs:** `../research/2026-06-19-glm5.2-jax-tpu-research-findings.md` and `../research/2026-06-19-glm5.2-explainer.md` (background). ⚠️ Those predate this spec and contain superseded claims (e.g. "no indexer ReLU", "interleaved MLA RoPE") — this spec is authoritative.

All architecture facts (§1.1–§4) were derived from the **real source** (transformers 5.9.0 `glm_moe_dsa`, vLLM `deepseek_v2.py`, the `claude-deepseek-v4` fork) and **re-verified 2026-06-19 against transformers 5.9.0 in a py3.11 venv**: rotate-half RoPE for *both* MLA and indexer (HF *docstrings* falsely say "interleaved" — `modeling_glm_moe_dsa.py:83,111`; trust the code), `F.relu` on indexer scores at `modeling_glm_moe_dsa.py:219`, no mscale branch, `k_norm=LayerNorm(eps 1e-6)`, and all §3 config defaults via `GlmMoeDsaConfig()`. They are device-agnostic and unchanged by the TPU-first strategy.

---

## 0. Strategy (TPU-first) — read this

Develop and validate **on the real v6e from the start**, running the **real Pallas/Mosaic kernels** — not CPU stand-ins. This reverses a prior CPU-first plan, driven by the prior **DeepSeek-V4** bring-up (`/home/enyouki/claude-deepseek-v4`), which was dominated by **multi-device, real-reshard-path bugs CPU structurally cannot reproduce** — chiefly **uninitialized-HBM-on-reshard ("S1")**: a device-side weight reshard read uninitialized HBM and baked per-process garbage into the weights, producing **coherent-looking-but-wrong** output at temp=0 (~30 sessions, root-cause hypothesis overturned repeatedly). This class does **not** appear single-device, and much of it doesn't appear under the CPU `interpret=True` path. So "develop on TPU" means **exercise the real kernels AND the real multi-device sharded weight-loader early** — not "run tiny tests on one chip."

**Three load-bearing guardrails the plan must keep:**
1. A pure-`jnp` fp32 reference **runnable on TPU** — the irreplaceable middle oracle. It runs on TPU not to improve math validation (math is device-independent) but because it is the **kernel's answer key**: the kernel only runs on TPU, so the reference must co-locate with it, else a CPU-vs-TPU fp32 reduction-order delta pollutes the one comparison meant to be clean.
2. **Staged triangulation** (§7) with a tight fp32 *math* gate distinct from a loose bf16 *kernel-noise* gate. Never collapse "kernel-on-TPU vs HF-eager" into one number.
3. **Multi-device** on the real v6e is a **required** gate (§7), not optional — and it must be **armed on a sharding geometry that actually triggers the S1 class** (§6 Phase 1b), not just any 4-chip run.

---

## 1. Goal & non-goals

### Goal
Bring up **GLM 5.2** — `GlmMoeDsaForCausalLM` (`model_type="glm_moe_dsa"`), i.e. **DeepSeek-V3.2-style MLA + DSA at GLM-5 scale** — as a **native-JAX** model in `tpu-inference`, **developed and validated on TPU with the real kernels from the start. DSA is implemented and validated, not deferred.**

The v1 finish line is **single-sequence correctness of the components on a scaled-down config, on TPU, single- and multi-device** — the dense MLA backbone, the MoE/router stack, and the DSA sparse path — validated against the in-tree `transformers GlmMoeDsaForCausalLM` eager forward via §7. **We do not run the full 78-layer model end-to-end in v1** (that and real-checkpoint generation are Phase 3+).

**Severable v1 fallback (schedule insurance):** if the Mosaic sparse kernel slips, v1 may ship as the **dense backbone (Phase 1) green single+multi-device, with DSA validated in jnp-ref form only** — indexer math + the §2 dense≡sparse equivalence + index-set parity — and the sparse *kernel* dropped to v1.1. The §2 equivalence is exactly what makes a kernel-free DSA milestone meaningful.

### Hardware & environment reality (prerequisite — see Phase 0)
- Host is **worker of a v6e-16 pod** (`ct6e-standard-4t-tpu`): **4 Trillium chips/host** via `/dev/vfio/{0,1,2,3}`; a single-host **4-chip mesh** is the multi-device test surface; the full 16-chip pod needs multi-host (Ray/Pathways), a later step.
- **The TPU is currently 100% occupied** by a running `gpt-oss-120b` vLLM job holding all 4 local chips. **Phase 0 must free a device before any kernel iteration** — and TPU-first removes the CPU escape hatch, so Phase 0 needs an explicit device-access fallback ladder and a named owner (§6 Phase 0).
- **Env:** system Python is 3.10 and **cannot** install the pinned `jax==0.10.1` (needs ≥3.11; repo Docker uses 3.12). No ready venv — Phase 0 stands one up. The persistent XLA **compilation cache is on by default** (`compilation_manager.py`), so edit→run is near-instant after the first cold compile.

### 1.1 What changed vs the (architecture-wrong) superseded spec — keep this
The original spec targeted `Glm4MoeLiteForCausalLM` = **GLM-4.7-Flash**, not GLM 5.2. Corrected and verified:
- **Class/identity:** `GlmMoeDsaForCausalLM` / `glm_moe_dsa`, config `GlmMoeDsaConfig(Glm4MoeLiteConfig)`. The Lite *config* has **no indexer** (the gate is `is_v32 = hasattr(config,"index_topk")`).
- **Scale:** GLM-5 (hidden **6144**, **78** layers, **256** experts, q_lora **2048**, **64** heads), not Lite ~30B.
- **RoPE:** **non-interleaved (NeoX / rotate-half)** for *both* main MLA and the indexer. ⚠️ The DSA docstrings *claim* "interleaved"; the **code is rotate-half** — trust the code (§4.5).
- **Indexer ReLU:** the indexer **applies `F.relu`** to the scores (`modeling_glm_moe_dsa.py:219`). (The companion research doc's "no ReLU" is wrong.)
- **`routed_scaling_factor`:** default **2.5**. **`num_experts_per_tok`:** **8**.
- **Layer schedule fields:** **no `scoring_func` / `first_k_dense_replace` field** on `GlmMoeDsaConfig`. Dense-vs-MoE from `mlp_layer_types` (HF) / `first_k_dense_replace` (vLLM/checkpoint); indexer recompute-vs-reuse from `indexer_types`. Scoring is hardcoded **sigmoid**.
- **Oracle:** the in-tree `transformers GlmMoeDsaForCausalLM` eager class (CPU, random weights, no download), not hand-transcribed numpy refs (those re-derived the code's own math — tautological).

### Non-goals (deferred past v1)
- **Full end-to-end model** (all 78 layers, real-checkpoint generation). Phase 3.
- **MTP** — the checkpoint's `model.layers.78` MTP head is **dropped on load** (`_keys_to_ignore_on_load_unexpected = [r"model\.layers\.78.*"]`).
- **Quantization** (fp8 UE8M0 / fp4). v1 runs bf16/fp32; the GPU DSA path is fp8 (DeepGEMM `fp8_mqa_logits`), v1 uses a bf16/fp32 indexer.
- **Aggressive kernel autotuning** — kernels are idiomatic-from-the-start; autotuning is bounded and post-correctness (§10).
- **Multi-batch / concurrent decode.** v1 is single-sequence (the fork has a **confirmed** `max_num_seqs>1` MoE-corruption bug — same uninit-HBM mechanism).

---

## 2. Architecture summary

GLM 5.2 is a decoder-only transformer where every layer is **MLA + DSA**, the FFN is **MoE** (after a few leading dense layers):

1. **MLA (Multi-head Latent Attention)** — cache one small **latent** (`kv_lora_rank=512` + shared `qk_rope_head_dim=64` rope key = 576 numbers) and **up-project** (`kv_b_proj`) to per-head K/V at attention time. Query path is compressed (`q_a_proj → q_lora_rank → q_a_layernorm → q_b_proj`). RoPE (rotate-half) applies only to the 64-dim rope slice; the attention splits **nope-first** `[qk_nope=192, qk_rope=64]`. Softmax scale = `qk_head_dim**-0.5 = 256**-0.5` (**no mscale** under default rope; §4.9). **No per-head QK-norm** — only `q_a_layernorm` and `kv_a_layernorm`.
2. **DSA (lightning indexer)** — a cheap auxiliary scorer ranks past tokens and selects the **top-`index_topk`=2048**; MLA then runs only over the selected set (additive `-inf` mask combined with the causal mask). V3.2/GLM form: per-head dot-product, **`relu`** on the scores, weighted head-sum, **no softmax**; `k_norm`=**LayerNorm** (the one LayerNorm in an otherwise RMSNorm model). The indexer splits **rope-first** `[64,64]` (opposite order to main attention) and **reuses the MLA query LoRA residual** `q_a_layernorm(q_a_proj(x))`. Some layers (`indexer_types[i]=="shared"`) **reuse the previous layer's top-k**.
3. **MoE** — 256 expert FFNs (`moe_intermediate_size=2048`) + 1 always-on shared expert; a router picks **top-8-of-256**. **sigmoid** scoring; `e_score_correction_bias` added **only for selection** (gathered weights use bias-free sigmoid scores); `norm_topk_prob` renormalizes; routed output scaled by `routed_scaling_factor` (default 2.5); the shared expert added **unscaled**. The first `first_k_dense_replace` (default 3) layers are a plain dense SwiGLU FFN.

**Key safety property (drives the validation ladder):** for any sequence of length ≤ `index_topk` (2048), `topk = min(index_topk, total_len)` selects **every** valid token, so DSA and dense attention produce the **identical** result. Sparsity only changes behavior past 2048 tokens. This lets us validate a correct **dense backbone** first (short seqs), then the **sparse path** (long seqs), with a clean equivalence check at the boundary — checkable **on TPU at fp32** (≈1e-6), isolating the entire sparse-plumbing bug class with no HF dependency.

---

## 3. Exact GLM 5.2 config (`GlmMoeDsaConfig`)

`GlmMoeDsaConfig` subclasses `Glm4MoeLiteConfig`; the generated config flattens inherited fields. **Defaults below verified live** via `GlmMoeDsaConfig()`. The real checkpoint's `config.json` is authoritative and overrides these; our config path reads everything from `hf_config`.

| Field | Default | Field | Default |
|---|---|---|---|
| `hidden_size` | 6144 | `n_routed_experts` | 256 |
| `num_hidden_layers` | 78 | `n_shared_experts` | 1 |
| `intermediate_size` (dense FFN) | 12288 | `num_experts_per_tok` | **8** |
| `moe_intermediate_size` | 2048 | `n_group` / `topk_group` | **1 / 1 (grouping OFF)** |
| `vocab_size` | 154880 | `routed_scaling_factor` | **2.5** |
| `max_position_embeddings` | 202752 | `norm_topk_prob` | True |
| `num_attention_heads` | 64 | scoring | **sigmoid (hardcoded; no `scoring_func` field)** |
| `num_key_value_heads` | 64 | `rms_norm_eps` | 1e-5 |
| `q_lora_rank` | 2048 | `attention_bias` | False |
| `kv_lora_rank` | 512 | `tie_word_embeddings` | False |
| `qk_nope_head_dim` | 192 | `qk_head_dim` (derived) | 256 |
| `qk_rope_head_dim` | 64 (`head_dim` attr → this) | `initializer_range` | 0.02 |
| `v_head_dim` | 256 | `hidden_act` | "silu" |

**RoPE config key is `rope_parameters`** (a dict), **not `rope_scaling`**. Default `{'rope_theta': 10000.0, 'rope_type': 'default'}` — **plain RoPE, no YaRN** by default. A real GLM-5 checkpoint may set `rope_type="deepseek_yarn"` with `factor`/`mscale_all_dim` (§4.9).

**Layer schedules** (two independent per-layer patterns, computed in `__post_init__`):
- **Dense-FFN vs MoE → `mlp_layer_types`**: if absent, `["dense"]*min(3,L) + ["sparse"]*(L-3)` ⇒ **first 3 dense**, rest MoE. vLLM reads the equivalent `first_k_dense_replace`. **Read whichever the checkpoint supplies** (prefer `first_k_dense_replace` if present, else derive from `mlp_layer_types`).
- **Indexer recompute vs reuse → `indexer_types`**: if absent, computed from `index_topk_freq` — a `"full"` layer at the cadence start, the rest `"shared"`. A `"shared"` layer reuses the previous layer's `topk_indices` (`skip_topk`). **Every attention layer is DSA-sparse**; "full"/"shared" only controls whether the *indexer scores* are recomputed. **Read from `hf_config`**; the scaled-down test config sets it with ≥1 `"shared"` layer to exercise the reuse path.

**DSA indexer fields:** `index_topk=2048`, `index_head_dim=128`, `index_n_heads=32`, `indexer_types` (computed). All read from `hf_config`; never hardcoded.

**Sentinels (DSA removes these → `AttributeError`):** `rope_interleave`, `pretraining_tp`. Never reference them.

**Norms — definitive:**
- `q_a_layernorm` = RMSNorm(`q_lora_rank=2048`, eps `rms_norm_eps`); `kv_a_layernorm` = RMSNorm(`kv_lora_rank=512`, eps `rms_norm_eps`).
- `input_layernorm`, `post_attention_layernorm`, final `model.norm` = RMSNorm(`hidden_size`).
- (indexer) `k_norm` = **LayerNorm**(`index_head_dim=128`, eps **1e-6**) — has **weight AND bias**. The only LayerNorm in the model.
- **No per-head q/k norm.**

---

## 4. GLM/DSA deltas vs the existing DeepSeek-V3 JAX path (the things to get exactly right)

The repo's `models/jax/deepseek_v3.py` is **plain DeepSeek-V3 (no indexer)**; its module globals are already close to GLM (`routed_scaling_factor=2.5`, `num_experts_per_token=8`, `first_k_dense_replace=3`, 256 experts). Each delta below is a known foot-gun.

1. **Config-driven hparams** — replace DeepSeek's hardcoded module globals (`deepseek_v3.py:76-108`) with values from `hf_config` (à la `qwen3_moe.py`). GLM differs in `n_group` (1 vs 8), `hidden_size` (6144 vs 7168), `q_lora_rank` (2048 vs 1536), `qk_nope_head_dim` (192 vs 128), `v_head_dim` (256 vs 128), `num_attention_heads` (64 vs 128), and adds the `index_*` fields.
2. **`n_group = topk_group = 1`** — grouping **off**; take top-8 from all 256. `DeepSeekV3Router.get_topk_indices` already skips the group branch when `n_groups==1`.
3. **Router gate in fp32**, **sigmoid** scoring, `e_score_correction_bias` added **before top-k for selection only**; **gathered weights use bias-free sigmoid scores**; `norm_topk_prob` renorm (`/ (sum + 1e-20)`).
4. **`routed_scaling_factor` (default 2.5) applied to the routed-expert output**, **not** to router logits; shared expert added **unscaled**. Site already correct in `SharedFusedMoe.__call__`; pass the config value through `DeepseekV2Moe`.
5. **Rotate-half (NeoX) RoPE for BOTH** main MLA and the indexer. **The single highest porting risk.** The repo's `DeepseekScalingRotaryEmbedding` is **interleaved** and the fork uses complex-multiply RoPE — so do **not** reuse either; the base `RotaryEmbedding` in `rope.py` is already rotate-half (subclass/reuse it). ⚠️ vLLM-GPU also applies **interleaved** MLA rope (`is_neox_style=False`, `deepseek_v2.py`) — a genuine HF-vs-vLLM divergence, so don't cross-port RoPE from vLLM either; only the **HF eager class** is rotate-half for MLA. Pin it bit-for-bit vs HF `apply_rotary_pos_emb` (§7). Main attention splits **nope-first** `[192,64]`; indexer splits **rope-first** `[64,64]`.
6. **silu only** for FFN/expert activation (gated SwiGLU).
7. **Dense-then-MoE schedule** — controlling field is `mlp_layer_types` (default first-3-dense); **no `first_k_dense_replace` field** on the HF config (it's the vLLM/checkpoint key). Config-driven; read whichever the checkpoint supplies (§3).
8. **No per-head QK-norm** (§3).
9. **DSA softmax scale = `256**-0.5` with NO mscale** under default rope. ⚠️ **HF-vs-vLLM discrepancy:** under YaRN, **vLLM** multiplies the scale by `mscale²` (`deepseek_v2.py`) but **HF DSA has no mscale branch**. With default rope both agree. v1 tests use **default rope** (no ambiguity); the YaRN `mscale²` is a **Phase-3 reconciliation risk** needing the real `config.json` (§8, §9).
10. **DSA indexer = V3.2/GLM form, NOT the fork's V4 form.** Keep: q-up-proj from the shared q-LoRA residual, **`relu` on scores**, per-head `weights_proj × n_heads**-0.5`, head-sum, `top_k`. **Drop** (V4-only): the **compressor/CSA** (GLM's K is direct `k_norm(wk(hidden_states))` per-token — the single biggest math change), SWA flavors, mHC sinkhorn, APE. `k_norm` is **LayerNorm**, not RMSNorm.
11. **PyTorch quirks — nothing to port:** do **not** carry over DeepSeek-V3's layer-0 residual `.clone()` or fp16-overflow rescale; the HF GLM-DSA port already dropped them (verified — `DecoderLayer.forward` is a plain residual add).

---

## 5. Code structure & reuse map (TPU-forward)

**New file `tpu_inference/models/jax/glm_moe_dsa.py`, reusing DeepSeek modules by import.**

> **TPU-forward notes:** (a) Implement **two** MLA paths and run **both on TPU**: a **pure-`jnp` non-absorbed `GlmMoeDsaAttention`** (the fp32 *oracle*/answer key, §7) and the **production absorbed MLA Pallas kernel** (`kernels/mla/v2`). (b) **RoPE has no flag**: the repo's `DeepseekScalingRotaryEmbedding` is hard-coded interleaved; GLM is rotate-half — and the base `RotaryEmbedding` in `rope.py` is already rotate-half, so subclass/reuse the base; do **not** use `DeepseekScalingRotaryEmbedding`. v1 uses default rope (no YaRN). (c) The production **`JaxAutoWeightsLoader` regex loader** is brought up **early** (not deferred), because the most expensive prior bug (S1, uninit-HBM-on-reshard) lives in the **multi-device sharded weight-load path** — the multi-device gate (§6, §7) needs it.

**Reuse as-is (import) from `deepseek_v3.py`** (re-locate by symbol; line hints are 2026-06-19): `DeepseekV3MLA` (587), `DeepseekV2Moe` (846), `DeepSeekV3Router` (989), `DeepseekV3MLP` (738), `SharedFusedMoe` (815)/`JaxMoE` (`moe/moe.py:129`), `DeepseekV3DecoderLayer` (942), `MLAEinsum` (488). Norms: `JaxRmsNorm`/`JaxLayerNorm` (`norm.py`). Linears: `JaxEinsum`/`JaxLmHead` (`linear.py`), `JaxEmbed` (`embed.py`), `UnquantizedConfig({})`. RoPE: base `RotaryEmbedding` (`rope.py`, rotate-half) — **not** `DeepseekScalingRotaryEmbedding`. Loader: `JaxAutoWeightsLoader` (`weight_utils.py:870`) via `LoadableWithIterator` — brought up early for the multi-device gate.

For the **absorbed MLA kernel path**, supply a **no-quant `kv_b` split** producing `k_up_proj`/`v_up_proj` (created during weight load, not random-init — `MLAEinsum.load_weights` asserts `quant_config is not None` at `deepseek_v3.py:525`, so write a **standalone** no-quant split rather than touching it).

**Write GLM-specific deltas only:**
1. **Config-driven hparams** read from `hf_config`, replacing DeepSeek's module globals.
2. **DSA `GlmMoeDsaIndexer` module + top-k selection** (the genuinely new piece; V3.2/GLM form per §3/§4).
3. **Rotate-half RoPE** wiring + GLM router constants.
4. **DSA sparse-attention kernel (Phase 2)** — **port the fork's already-wired Mosaic `sparse_attn` kernel** and adapt: drop `attn_sink`, drop the compressor (direct `k_norm(wk(x))`), keep the one-hot-matmul gather (the fork hit `E2003 UnprovenMemoryAccessAlignment` on v6e and fixed it this way, commit `ace9d576`), `-1` sentinel = ignore. ⚠️ The kernel's in-file docstring says "not yet wired" but that is **stale** — the fork wired it into both prefill/decode call sites via `shard_map` (commit `c2557e26`); **port that wiring, don't reinvent it.** The kernel is still **non-paged / reads all N**, so give it the paged/flattened KV surface.

**Surgical, behavior-preserving param-izing of DeepSeek modules:** `DeepseekV2Moe.__init__` (and the `SharedFusedMoe(...)` block, incl. the `expert_axis_name` global — `deepseek_v3.py:908`) read ~10 module globals. Add optional kwargs defaulting to current globals so existing DeepSeek behavior is **unchanged** (existing DeepSeek construction tests stay green as a gate).

**DSA hook point:** indexer + additive `-inf` mask insert into the new `GlmMoeDsaAttention.__call__` for the jnp-ref path (a `dsa` flag toggles dense vs sparse); on the kernel path the hook is `DeepseekV3MLA.compute_attention` (`deepseek_v3.py:663`) immediately before `mla_attention(...)` (`:702`), threading `topk_indices` into the kernel.

**New runtime scaffolding for DSA (Phase 2):** an **indexer-key KV cache** (raw per-token keys `[tokens, index_head_dim=128]`, single-head/MQA, distinct from the MLA latent cache); a **`topk_indices` field added to `AttentionMetadata`** (`attention_metadata.py`) — `int32`, shape `[tokens, index_topk]`, `-1` sentinel, **does not exist today**; runner-side population; threading runner → `mla_attention` → kernel scalar-prefetch.

**Kernel-precision wrapper:** `mla_attention` / `attention_interface.py:565-579` calls the MLA kernel **without** `s_dtype`/`p_same_dtype_as_v`/`two_step_flash_attention`, so they fall to the kernel's bf16 defaults (`kernels/mla/v2/kernel.py:2165,2168`). **Thread them through that call site** so the kernel can run **fp32** for the tight algebra check, then bf16 for the shipped check.

**Registration:** `_MODEL_REGISTRY["GlmMoeDsaForCausalLM"] = GlmMoeDsaForCausalLM` in `models/common/model_loader.py` (inline-import pattern `:73-100`); add the arch to `_PP_DISABLED_MODELS` (`:60-61`). If a checkpoint declares a custom `auto_map` arch string, map it to the same class.

**Real kernels (run on TPU from the start):** MLA `kernels/mla/v2/kernel.py` (auto-pads head dims to 128-mult; flexible runtime blocks; fp32 output special-cased `:1938-1939`; bf16 intermediates default via `s_dtype` `:2165` / `p_same_dtype_as_v` `:2168`); microtest pattern `tests/kernels/mla_v2_test.py:49-151` (≈9 synthetic arrays, no mesh/metadata/runner).

**DSA source (adapt V4→V3.2/GLM) — `claude-deepseek-v4` fork** (real checkout under `work/tpu-inference/tpu_inference/`): JAX indexer `layers/jax/attention/deepseek_v4_attention.py:447-505` (keep relu `:484`/weights/top_k; **drop compressor** `:475`, replace with direct `k_norm(wk(hidden))`); Mosaic sparse kernel `kernels/sparse_attn/kernel.py` (one-hot gather, `-1` sentinel, `attn_sink` to drop — already wired via `c2557e26`); parity-harness blueprint `tests/models/jax/test_deepseek_v4.py` + `_deepseek_v4_reference/`. **Hard-won TPU lessons (S1 etc.):** host-stack weights straight into the sharded layout (no device reshard — the actual S1 fix, commit `5a3ed435`); wrap Mosaic kernels in `shard_map`; move RoPE-freq precompute to host NumPy. **Do NOT** use `gmm_v2(zero_initialize=True)` as an S1 mitigation — the fork tested it and **disproved** it (commits `65bc1858`/`a0eb101b`).

**Authoritative oracle — transformers 5.9.0 `modeling_glm_moe_dsa.py`:** `GlmMoeDsaIndexer` (relu **219**, weights 215, top-k 226-228), `GlmMoeDsaAttention` (scale `256**-0.5`, indexer→additive mask), `apply_rotary_pos_emb` (rotate-half, ~67-102), `GlmMoeDsaTopkRouter`, `GlmMoeDsaMoE` (routed_scaling 581), `GlmMoeDsaNaiveMoe` (independent eager MoE ref), `GlmMoeDsaForCausalLM`.

**vLLM (GPU reference, read-only):** `models/deepseek_v2.py` — `is_v32` gate, `Indexer`, yarn `mscale²`, `topk_tokens=index_topk`, `packed_modules_mapping` fused names (`fused_qkv_a_proj`, `wk_weights_proj`); `layers/sparse_attn_indexer.py` (fp8 path). *(Re-locate by symbol — vLLM line numbers drift.)*

**Weight names** (HF layout, prefix `model.layers.{i}.`) — our loader maps the **unfused HF names** (vLLM's `fused_qkv_a_proj`/`wk_weights_proj` are internal post-load fusions, NOT on disk):
- **Attention:** `self_attn.{q_a_proj, q_a_layernorm, q_b_proj, kv_a_proj_with_mqa, kv_a_layernorm, kv_b_proj, o_proj}.weight` — `kv_a_proj_with_mqa` concatenates `[kv_lora_rank(512) | qk_rope_head_dim(64)]`; split `kv_b_proj`→`k_up`/`v_up`.
- **Indexer:** `self_attn.indexer.{wq_b, wk, weights_proj}.weight` + `self_attn.indexer.k_norm.{weight, bias}` (LayerNorm ⇒ has bias).
- **MoE (sparse layers):** `mlp.gate.weight` + `mlp.gate.e_score_correction_bias`; **fused** experts `mlp.experts.gate_up_proj` (3-D `[E, 2·moe_inter, hidden]`, **split gate/up**) and `mlp.experts.down_proj` (3-D `[E, hidden, moe_inter]`); `mlp.shared_experts.{gate_proj, up_proj, down_proj}.weight` (**un**fused).
- **Dense layers (0..first_k_dense_replace-1):** `mlp.{gate_proj, up_proj, down_proj}.weight` (un-fused).
- **Top-level:** `model.embed_tokens.weight`, `model.norm.weight`, `lm_head.weight` (**untied**). Drop `model.layers.78.*` (MTP).

**Other repo references** (re-locate by symbol): `qwen3_moe.py` (config-driven template); `model_loader.py:60-61,73-100`; `moe/{moe.py:129,dense_moe.py}`; `{norm.py,linear.py,rope.py}`; `attention_metadata.py`, `sharding.py`; `conftest.py:46-61` (single-device mesh assert); `test_deepseek_v3.py:40` (broken `DeepSeekV3WeightLoader` import — Buildkite `--ignore`s it; use construction tests, not this, as the DeepSeek regression gate); `compilation_manager.py` (compile cache); `envs.py`. Kernel tuner for §10: `tools/kernel/tuner/v1/`; production lookup `kernels/mla/v2/tuned_params.py`; regression gate `tests/kernels/mla_tuned_vs_baseline_test.py:131-135`.

---

## 6. Phase plan (TPU-first)

### Phase 0 — TPU env, HF oracle, harness
**Deliverable:** a working TPU JAX environment and a harness whose oracle is the in-tree `transformers GlmMoeDsaForCausalLM` eager forward (torch, CPU).
- **Free a TPU device** — and because TPU-first removes the CPU progress escape hatch, follow an explicit **fallback ladder** (and name who owns each step): (1) confirmed authority to stop the `gpt-oss-120b` job → (2) an identified free pod worker → (3) multi-host pod scheduling → (4) a *bounded* CPU-`interpret` bridge for **pure-math unit tests only** (explicitly NOT a validation surface). Then stand up a **Python 3.12 venv**: `jax[tpu]==0.10.1`, `libtpu`, `flax`, `transformers`(≥5.9.0), `torch`, `numpy`, `pytest`, `pytest-mock`. Verify `jax.devices()` shows the v6e chips.
- **HF-eager oracle fixture** (torch CPU, random weights, no download): `GlmMoeDsaForCausalLM.from_config(cfg, attn_implementation="eager")`, **`experts_implementation="eager"`** (force the genuine per-expert loop, not fused `grouped_mm`), deterministic init **including buffers** (`e_score_correction_bias` — else the bias-for-selection delta passes vacuously).
- **Tiny `GlmMoeDsaConfig`** — min-viable dims that still exercise the real kernels: `hidden=512`, `layers=4` (≥1 dense + ≥1 MoE + ≥1 indexer "full" + ≥1 "shared"), `heads=8`, `index_topk=64`, `n_routed_experts=8`, `num_experts_per_tok=2`, `page_size=16`, **default rope**. Keep `kv_lora_rank=512 / qk_rope=64 / qk_nope=192 / v_head_dim=256 / index_head_dim=128` at **real** values (they auto-pad identically, cost ~nothing). Two seq regimes: ~32 (≤topk, dense-equiv) and ~128 (>topk=64, true sparsity).
- **Harness helpers:** a `t2j` weight converter (**transpose every linear** — JAX `x@kernel (in,out)` vs HF `nn.Linear (out,in)`; `embed_tokens` excepted, `lm_head` transposes; split fused `gate_up_proj`), a `maxabs` helper (upcast to fp32 **inside** maxabs), and an identical-weights checksum asserted before every parity run.
- **Mesh fixtures:** provide **both** a 1-device fixture and a **multi-device (4-chip)** fixture (do not inherit the single-device assert in `conftest.py`). Do **not** depend on `test_deepseek_v3.py` (broken import) — use construction tests for the DeepSeek regression gate.
- **Exit:** `jax.devices()` sees the v6e; the harness collects on TPU; the HF oracle runs a finite forward on the tiny config; a trivial JAX op round-trips TPU→host numpy.

### Phase 1 — Dense-MLA GLM backbone on TPU. Split into 1a (single-device) → 1b (multi-device); **1a hard-gates 1b** (never debug math on the mesh).

**Phase 1a — single-device correctness (real kernel + fp32 jnp-ref oracle).**
- Param-ize `DeepseekV2Moe`/`SharedFusedMoe` (incl. `expert_axis_name`) behavior-preservingly (§5).
- **Pin RoPE bit-for-bit** (rotate-half) vs HF `apply_rotary_pos_emb` — its own unit test (exact/1e-6).
- Build **both** MLA paths on single-device TPU: the pure-`jnp` non-absorbed `GlmMoeDsaAttention` (fp32 oracle) and the absorbed `mla/v2` kernel (with the no-quant `k_up`/`v_up` split). Thread `s_dtype`/`p_same_dtype_as_v`/`two_step_flash_attention` (§5) so the kernel can run fp32 (algebra check) then bf16 (shipped check).
- **Triangulated per-submodule parity** (§7): HF-fp32(CPU) vs jnp-ref-fp32(TPU) = **math gate**; jnp-ref-fp32 vs kernel = **kernel gate**. Covers router, RMSNorm/LayerNorm, dense FFN, MoE experts (vs HF eager `GlmMoeDsaNaiveMoe`), embed/lm_head, MLA.
- **Weight-mapping golden test:** load the HF `state_dict` via the converter; assert every param maps (no `KeyError`, no leftover random-init), fused `gate_up_proj` split correct, indexer names present, `layers.78` dropped.
- **Exit (1a):** full backbone forward on the tiny config, seq < `index_topk` — fp32 **math gate** at rtol≈1e-3 vs HF; bf16 **shipped gate** within empirical kernel tolerance (~5e-2…2e-1) + argmax ≥0.95; weight-map golden passes. All single-device.

**Phase 1b — multi-device equality gate (the gate that catches the S1/uninit-HBM/reshard class).**
- Run the dense backbone on the **4-chip mesh via the real `JaxAutoWeightsLoader`**; **NaN-poison HBM** before load and assert no NaN/garbage leaks into outputs; assert **N-device fp32 result == 1-device fp32 result** (sharding is value-invariant; compare 1/2/4-device); exercise `expert_axis_name` sharding.
- **Arm the gate on a triggering geometry, not just the tiny config.** The tiny config's tiny, evenly-divided shards may **not** reproduce S1 (a real-sized reshard / idle-shard / expert-partition phenomenon), so the gate could pass vacuously. Add a dedicated **sharding-geometry stress fixture** — expert count **not divisible** by the mesh (≥1 empty shard), head dims chosen to stress the reshard tiling — run through the NaN-poison + N-dev==1-dev checks (it need not pass full HF math parity; its job is to arm the S1 gate).
- **Exit (1b):** multi-device == single-device at fp32; stress fixture clean (no NaN/garbage, value-invariant). De-risks ~80% of the model and brings up the multi-device gate.

### Phase 2 — DSA (lightning indexer + sparse selection) on TPU
**Deliverable:** the DSA indexer + top-k selection (V3.2/GLM form) **and the adapted sparse-attention kernel**, built and validated on TPU, single- and multi-device.
- Implement `GlmMoeDsaIndexer` (§3/§4.10): `wq_b` from the shared q-LoRA residual, `wk`+`k_norm`(LayerNorm), `weights_proj`, rope-first split, rotate-half RoPE, `relu` on scores, weighted head-sum, `top_k=min(index_topk, T)`. Support `indexer_types=="shared"` (reuse previous top-k). Keep a pure-`jnp` indexer + `-inf`-mask sparse path as the fp32 oracle.
- **Port + adapt the fork's already-wired Mosaic `sparse_attn` kernel** (§5): drop `attn_sink`, drop compressor, keep one-hot gather, `-1` sentinel; wire it to the new indexer-KV cache + `topk_indices` in `AttentionMetadata` and give it the paged surface.
- **Indexer parity:** top-k **boundary-aware, tie-tolerant set equality** (a mismatch is acceptable only if swapped tokens straddle the cutoff AND their *fp32* scores are within a tie-band; interior or well-separated boundary swaps are hard reds) + `index_scores` at 1e-3 fp32 (computed from HF submodules — HF's indexer returns indices only).
- **Validation ladder vs HF eager (on TPU):** (1) seq ≤ `index_topk`: **sparse == dense at fp32 (≈1e-6)** — the §2 equivalence, cheapest sparse-plumbing gate, no HF dependency — and top-k contains all valid tokens; (2) seq > `index_topk`: full backbone forward, fp32 math gate vs HF (1e-3) + bf16 shipped gate + argmax ≥0.95, and the selected index set matches HF (tie-tolerant).
- **Multi-device sparse gate:** rerun the ladder on the 4-chip mesh; N-device == 1-device at fp32.
- **Exit:** both ladder checks pass vs HF eager on TPU, single- and multi-device.

### Phase 3 — Real-checkpoint reconciliation & hardening (needs real `config.json` / weights)
**Deliverable:** single-sequence GLM 5.2 correctness toward the real checkpoint.
- **Reconcile the YaRN `mscale²` HF-vs-vLLM discrepancy** (§4.9) against the real `config.json` and vLLM-GPU — in **fp32**, never judged only at bf16 (a near-1.0 `mscale²` hides under bf16 noise). Reconcile vLLM's fp8 indexer path (ReLU location, fp8 `mqa_logits`).
- Extend to the **full 16-chip pod** (multi-host via Ray/Pathways) and real expert/tensor parallelism beyond the single-host 4-chip gate.
- The pure-`jnp` references from P1/P2 remain the **kernel oracles**.
- **Still deferred/out:** fp8/fp4 quant, the multi-batch MoE bug, full 78-layer end-to-end generation, MTP.
- **Exit:** single-sequence generation matches the reference within bf16 tolerance on real shapes.

---

## 7. Testing & validation strategy

**Oracle = the in-tree `transformers GlmMoeDsaForCausalLM` eager forward** (torch, CPU, random weights, no download) — an **independent** implementation of every §4 delta. It runs on the host; the model-under-test runs on TPU; comparison is a **host-side `maxabs`** (JAX arrays → host numpy). Upcast both sides to fp32 **inside** `maxabs` (a bf16 TPU output returns as numpy bf16 — load-bearing). No `jax_enable_x64`.

### The triangulation (the core of the strategy)
Three implementations, three single-delta comparisons — never a fused HF-vs-bf16-kernel verdict:

```
HF-eager (CPU, fp32)  ──[MATH gate, 1e-3]──►  jnp-ref (TPU, fp32)  ──[KERNEL gate]──►  Pallas kernel (TPU)
        independent oracle                      our answer key                          shipped path
```

- **MATH gate** (HF-fp32 vs jnp-ref-fp32-on-TPU, rtol≈1e-3): catches every §4 math/spec bug (RoPE convention, head splits, `256**-0.5` scale, kv_b split, router bias-for-selection, routed_scaling 2.5, relu placement). This is the gate with *teeth* — prove it by an **injected-1%-error test** (perturb a scale, assert the fp32 gate catches it). Without an fp32 reference on TPU there is **no** math gate: the bf16 kernel noise floor (~4e-3/element, compounding over depth) is larger than a real ~1% bug.
- **KERNEL gate** (jnp-ref vs kernel, on TPU): run the kernel **twice** — (a) fp32 (`s_dtype=fp32, p_same_dtype_as_v=False`) vs jnp-ref-fp32 at tight tolerance to check kernel *algebra*; (b) bf16 shipped defaults vs jnp-ref-bf16 at the **empirically-measured bf16 floor** (run the jnp-ref in bf16 to derive the floor — don't guess). Repo precedent: bf16 attention ~0.1–0.2 (`ragged_paged_attention_kernel_v2_test.py:129-134`, `mla_v2_test.py:405`); MoE/elementwise ~5e-2.
  - ⚠️ The kernel gate carries **double duty**: the non-absorbed jnp-ref deliberately does NOT absorb, so a bug in the **kv_b absorption** algebra shows up only here and the jnp-ref can't catch it. Give the **fp32 kernel-algebra check the same teeth** as the math gate: a **second injected-error test that perturbs the kv_b absorption split** and asserts the fp32 kernel gate catches it.

### Tiered comparison (each submodule at the layer where it lives, plus full forward)
| Check | Tolerance / method |
|---|---|
| RoPE (bit-for-bit) | exact / 1e-6 |
| Router | top-k indices **exact** (fp32 gate, well-separated scores) + weights 1e-2 |
| RMSNorm / LayerNorm, dense FFN, embed/lm_head | 1e-3 (fp32 math gate) |
| MLA attention | math gate 1e-3 (jnp-ref vs HF); kernel-algebra 1e-3 fp32 (+ absorption injected-error test); shipped bf16 at measured floor |
| MoE experts (vs **eager** `GlmMoeDsaNaiveMoe` — assert `config._experts_implementation == "eager"`) | 1e-2 |
| Indexer | **boundary-aware tie-tolerant** top-k set equality + index_scores 1e-3 fp32 |
| Dense==sparse (§2, seq ≤ topk) | **fp32 ≈1e-6 on TPU** — isolates sparse plumbing, no HF dependency |
| Full forward | per-layer index-**set** equality (tie-tolerant) + fp32 math gate 1e-3 / bf16 shipped at floor + argmax ≥0.95 |

### Multi-device (required gate, not optional)
The bug class that motivated TPU-first lives here. On the **4-chip mesh**, via the **real sharded weight-loader**:
- **N-device fp32 result == 1-device fp32 result** (sharding must be value-invariant). Compare 1/2/4-device.
- **NaN-poison HBM** before weight load; assert no NaN/garbage leaks into outputs (catches the S1 uninit-HBM-on-reshard read). The effective fix is **host-stacking weights straight into the sharded layout** (no device reshard); `gmm_v2(zero_initialize=True)` was tried in the fork and **disproven** — do not rely on it.
- Run the gate against the **sharding-geometry stress fixture** (§6 Phase 1b), not just the tiny config, or it may pass vacuously. Exercise `expert_axis_name` partitioning. **A single-device v6e pass is NOT TPU validation.**

### Determinism & ties
TPU is seeded-reproducible but **not bit-equal** to the torch ref; reductions are non-associative and order differs (more so multi-device). Top-k ties are a documented hazard (`fused_moe_v1_test.py:299-300`, jax #34620). Hence: the fp32 math gate uses well-separated scores where exact equality is asserted; the indexer set compare is **boundary-aware tie-tolerant** (adjudicate boundary swaps by recomputed fp32 scores); `index_scores` gated separately at 1e-3 so a relu/scale bug shows up even when the set survives. Pin the torch ref: fixed seeds, single-process (`WORLD_SIZE=1`), fp32, eager.

---

## 8. Risks & mitigations

| Risk | Mitigation |
|---|---|
| **Uninit-HBM / reshard / SPMD bugs** (S1 class) — silent, coherent-but-wrong; CPU-invisible; ~30 sessions last time | Multi-device gate from **Phase 1b** (§7): real sharded loader on 4-chip mesh, N-dev==1-dev at fp32, NaN-poison HBM, host-stack-into-sharded-layout. |
| **Tiny config doesn't reproduce S1**, so the gate passes vacuously | **Sharding-geometry stress fixture** (§6 Phase 1b / §7): uneven expert/mesh divisibility, ≥1 empty shard, real-ish reshard tiling — arms the gate on a triggering geometry. |
| **Unattributable TPU failures** (math vs kernel vs sharding vs runtime) | **Triangulation** → §7. |
| **bf16 tolerance inflation hides a real ~1% math bug** | fp32 math gate distinct from bf16 gate; derive the bf16 floor empirically; **injected-1%-error test**; argmax is a backstop, not the verdict. → §7. |
| **kv_b absorption bug invisible to the jnp-ref** (only the kernel absorbs) | Dedicated **fp32 kernel-algebra gate** + **absorption injected-error test** → §7. |
| **Top-k set equality flaky under bf16/multi-device reduction order** | Boundary-aware tie-tolerant set compare; separate `index_scores` 1e-3 gate → §7. |
| **YaRN `mscale²` HF-vs-vLLM divergence** (§4.9) | v1 uses default rope (both agree); Phase-3 task in **fp32** vs real `config.json` (§9). |
| **RoPE convention** — GLM is rotate-half; the repo's `DeepseekScalingRotaryEmbedding`, the fork, AND vLLM-MLA are all interleaved | Reuse the base rotate-half `RotaryEmbedding`; **pin bit-for-bit vs HF** as its own test → §4.5. |
| **Weight fusion / transpose mismatch** | Loader maps **unfused HF names**, splits `gate_up_proj`; **weight-map golden test** + identical-weights checksum + per-submodule fp32 parity. |
| **Param-izing DeepSeek globals regresses DeepSeek** (esp. `expert_axis_name`) | Optional args defaulting to current globals; **construction regression tests**; don't depend on broken `test_deepseek_v3.py`. |
| **Absorbed MLA needs `k_up`/`v_up`** (load-time; asserts `quant_config is not None`) | Standalone **no-quant `kv_b` split**; leave `MLAEinsum.load_weights` untouched → §5. |
| **Adapted sparse kernel** — non-paged, carries `attn_sink`, may hit tile-alignment; risk it drags v1 | Treat as a **port+adapt** of the fork's already-wired kernel (§5); validate kernel-vs-jnp-ref; **severable v1 fallback** (DSA jnp-ref only, kernel → v1.1, §1). |
| **Device contention / env friction** — TPU busy; py3.10 can't install jax; no venv; no CPU escape hatch | Phase 0 device-access **fallback ladder** + named owner; py3.12 venv; compile cache reused → §6 Phase 0. |

---

## 9. Open questions

1. **YaRN `mscale²` (the genuine real-checkpoint blocker):** does the released GLM-5.2 `config.json` use `rope_type="deepseek_yarn"`? If so, HF eager (no mscale) and vLLM-GPU (`×mscale²`) **disagree** on the attention softmax scale. **Resolution needs the real `config.json`** (not yet on this machine). v1 sidesteps via default-rope tests; first Phase-3 item, resolved in fp32.
2. **Do we have the real GLM-5.2 checkpoint / `config.json` at all?** Determines when Phase 3 can start. The multi-device *reshard* gate does **not** need real weights (S1 reproduces on the sharded loader with random weights + the stress fixture), so it is not blocked by this.
3. **Dense-schedule key the real checkpoint uses** (`mlp_layer_types` vs `first_k_dense_replace`): config-driven (§3) — read whatever the checkpoint supplies.
4. **vLLM fp8 indexer ReLU location:** resolved for v1 (HF + fork = relu applied, verified at `:219`); confirm against vLLM-GPU in Phase 3.

Resolved decisions: native-JAX; **TPU-first with real kernels**; **pure-`jnp` fp32 reference on TPU** as oracle; **multi-device a first-class early gate (Phase 1b)**; dense-then-DSA phasing; DSA **implemented in v1** (with a severable kernel fallback); HF-eager oracle + identical weights; new file reusing by import. Architecture definitives (verified): rotate-half RoPE both paths, indexer applies ReLU, no per-head QK-norm, `k_norm`=LayerNorm, no mscale under default rope. Kernel perf = idiomatic now, bounded tuning later (§10).

---

## 10. Kernel optimization — idiomatic now, bounded tuning later

Performance is pursued in two modes; neither is a continuous autonomous loop during correctness bring-up.

**Now (free, during bring-up):**
- Write kernels in the **performant TPU idiom from day 1** (sensible block/tile structure, avoid pathological layouts, `shard_map`-wrapped Mosaic, host-side RoPE-freq precompute).
- **Reuse existing tuned MLA params** via `get_tuned_params` (`tuned_params.py`) where shapes match.
- **One-time structural perf smoke for the DSA sparse kernel** at **real `index_topk=2048`** before its kernel spec is frozen — a single "is the one-hot-gather structure within an order of magnitude of viable?" measurement (not a sweep), so a structural cliff is caught while a rewrite is still cheap. (MLA needs no such smoke — its structure is proven.)

**Later (bounded, post-correctness):** *autotuning* starts only **after single-sequence correctness is green and the kernel spec is frozen** (notably the YaRN `mscale²` question, §9). Then:
- Run **bounded, human-triggered sweeps** at **real GLM-5.2 dims** (tiny-config perf is meaningless — the serving lookup is exact-shape-keyed) using the existing tuner (`tools/kernel/tuner/v1`, `--run_locally=True`).
- For DSA, **write a dedicated `sparse_attn` tuner** (subclass `KernelTunerBase`) once the kernel is correct — the existing tuner is MLA-decode-only with no feedback loop.
- **Guardrails (mandatory):** a tuned config is accepted only if the **full correctness suite still passes**; a **tuned-vs-baseline regression test** (≤5% slower, modeled on `mla_tuned_vs_baseline_test.py:131-135`, extended to GLM/DSA shapes); tuning runs against a **frozen kernel commit**; apply-back is **human-reviewed**, never silently mutated.
- **Stop criterion:** discrete bounded sweeps — stop when the best config is within timing noise (<3–5%) of the previous best, or at a fixed TPU-hour budget.
