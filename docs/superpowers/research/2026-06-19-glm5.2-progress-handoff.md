# GLM 5.2 JAX/TPU — Progress Handoff (resume point)

**Date:** 2026-06-19. **Why this file:** main-thread context hit 200k; this captures everything needed to resume in a fresh session.

## ⚠️⚠️ ARCHITECTURE IDENTITY CORRECTED (read FIRST — supersedes everything below until the `## VERIFIED:` section)
**The user was right: `Glm4MoeLiteForCausalLM` is NOT GLM 5.2.** It's **GLM-4.7-Flash** (`model_type=glm4_moe_lite`, `zai-org/GLM-4.5` marker, MLA-only, 2048/47/64). **GLM 5.2 = `GlmMoeDsaForCausalLM`** (`model_type=glm_moe_dsa`, `zai-org/GLM-5` marker, MLA+DSA, 6144/78/256). Full evidence in the `## VERIFIED:` section at the bottom.

**Consequence: the approved spec, the written plan, AND the validation/oracle sections of THIS handoff above all target the WRONG class.** Rework on resume:
- **Identity (spec §3; explainer:138; research-findings:139-140):** target `GlmMoeDsaForCausalLM` / `glm_moe_dsa`, not Lite.
- **Config dims (spec §3 table):** GLM-5 scale (hidden 6144 / 78 layers / 256 experts), NOT Lite ~30B (2048/47/64). Real checkpoint config.json still authoritative; not on this machine.
- **RoPE (spec §3/§4):** GLM 5.2 DSA **removes `rope_interleave`** → **non-interleaved** RoPE. Spec's `rope_interleave=True` is Lite/4.7 behavior — WRONG for 5.2; RoPE is NOT a free transfer from Lite.
- **Code file name (spec §5):** not `glm4_moe_lite.py`; mirror the DSA arch.
- **Oracle:** full GLM 5.2 = `GlmMoeDsaForCausalLM`; **Phase-1 dense** = the DSA class with the indexer **disabled** (its true dense backbone). `Glm4MoeLite` is only a *proxy* for the shared MLA/MoE/router math (same code), differing in RoPE + scale.
- DSA indexer **applies `relu`** (confirmed in the real GLM-5 class) → spec §3/§4 "no ReLU" is WRONG.

**Silver lining:** `GlmMoeDsaConfig(Glm4MoeLiteConfig)` — DSA *subclasses* Lite, reusing the Lite decoder layer + Glm4Moe MoE/router/RMSNorm. So the ~80%-reuse thesis and dense-first/DSA-second phasing still hold; the *shared* MLA/MoE/router math is identical. Corrections needed = right class/config/scale + non-interleaved RoPE + indexer present (with ReLU) + oracle = GlmMoeDsa.

## Where we are
- **Spec (APPROVED):** `docs/superpowers/specs/2026-06-19-glm5.2-jax-tpu-design.md`.
- **Plan (WRITTEN, 12 tasks/60 steps):** `docs/superpowers/plans/2026-06-19-glm5.2-cpu-dense-glm.md`. Scope = Phase 0 (CPU env) + Phase 1 (dense MLA, no DSA). Self-review passed (no placeholders, coverage complete, consistent).
- Neither file is committed; repo is on branch `main`, `docs/superpowers/` untracked. Commit only on user's say-so (branch off main first).

## Locked decisions
Native-JAX · dense-MLA first / DSA second · no MTP/quant/multi-batch/perf in v1 · single-seq correctness CPU→TPU · code structure = NEW `models/jax/glm4_moe_lite.py` reusing DeepSeek modules **by import** + surgical config-driven param-izing (behavior-preserving for DeepSeek) · NO per-head QK-norm · first plan = P0+P1 only (P3 needs TPU hardware, becomes its own plan).

## Validation approach — CONVERGED, edits PENDING
Original spec said "validate vs HuggingFace-transformers-eager." **Infeasible:** transformers ships no accessible `Glm4MoeLite`/DeepSeek-V3 eager class, and we won't download a checkpoint. **New approach (locked):**
1. Per-submodule numeric parity vs numpy/torch references **transcribed from `../vllm`'s GLM source** (cite exact lines from `model_executor/models/glm4_moe_lite.py`, `models/deepseek_v2.py`).
2. Compose those into a **full numpy reference forward**; assert JAX full forward matches (not a pure-`jnp` self-oracle).
3. Run real vLLM submodules standalone on CPU where trivially possible.
4. **Phase-3 vs real vLLM (GPU)** = end-to-end backstop. Do NOT attempt full vLLM forward on CPU.
**TODO on resume:** edit spec §7 (+ any "HF-eager" mentions in §1/§10 and the explainer's "decisions locked" line) and plan Tasks 4–11 to reflect this. Then offer execution choice (subagent-driven vs inline).

## Adversarial review (user-requested) — 1 of 4 reported
Four adversarial agents launched to red-team the validation strategy. **3 still running**; their findings will arrive as task-notifications — collect and fold into synthesis:
- Reference-trust skeptic (`a0c6bde2f2723fbf4`) — pending.
- Better-oracle explorer (`a8ade8f70a24db5e0`) — pending.
- Missed-bug analyst (`afbc59687aae1b0cb`) — pending.
- **Leaner-path pragmatist (`a93c815040f45188c`) — DONE, findings below.**

### Pragmatist findings (captured)
- **Reused DeepSeek modules need ZERO source changes:** Router (`deepseek_v3.py:989-1008`), MLP (`738-811`), MLA (`587`, dataclass fields) are fully constructor-parameterized.
- **Only real refactor = `DeepseekV2Moe.__init__` (`846-856`)**: body (`859-931`) reads 10+ module globals and hardcodes `topk_groups=4` (`863`) and `norm_topk_prob=True` (`864`). Plan Task 3's kwarg list covers these — just confirm all ~10 are threaded.
- **Correction to an earlier "gap":** `SharedFusedMoe.__call__` (`826-837`, esp. line `830`) **unconditionally** applies `final_hidden_states *= self.routed_scaling_factor` to the expert **OUTPUT**, then adds the unscaled shared expert. There is NO `apply_routed_scale_to_output` flag in this file version — the application *site* is already correct for GLM. So GLM delta #1 reduces to passing the right **value** (1.8 vs DeepSeek's 2.5) through `DeepseekV2Moe`→`SharedFusedMoe`. Keep a 1.8-on-output assertion (verifies the value flow), but it's not a missing-flag bug.
- **Leaner plan proposed:** cut Tasks 5, 6, and the numpy-reference halves of 7/8/11/12 (they re-test shipping DeepSeek primitives). KEEP: venv/harness; the one real refactor (param-izing `DeepseekV2Moe`); router-delta test; 1.8-on-output assertion; construction + registry; one full-forward golden snapshot. → ~6 tasks, ~half the test code, every GLM *delta* covered on CPU before the real vLLM-on-GPU oracle in Phase 3.
- **Tension to resolve in synthesis:** pragmatist wants to cut the from-scratch numpy refs (YAGNI, since modules are reused); the reference-trust/missed-bug lenses may argue some of those refs are exactly what catches weight-mapping/transcription bugs. Decide per-test: keep refs that exercise GLM *deltas* and the *weight mapping*; cut refs that merely re-verify unchanged DeepSeek math.

## ⚠️ MAJOR UPDATE — better-oracle agent (`a8ade8f70a24db5e0`) REPORTED: the premise was FALSE
**"No HF-eager GLM class" is WRONG.** The agent BUILT AND RAN `Glm4MoeLiteForCausalLM` eager-forward on THIS Mac CPU with random weights — no download, no `trust_remote_code`.
- **BEST ORACLE (use this, supersedes numpy-transcription):** in-tree `transformers==5.9.0` `Glm4MoeLiteForCausalLM` via `from_config(...)` + `attn_implementation="eager"`. Satisfies the pin (`requirements.txt:27 → transformers>=5.8.0`), auto-registered, implements every §4 GLM delta. Replaces ~5 hand-written numpy refs with ONE authoritative full-forward reference. **Effort: SMALL (less than the numpy path).** Validation = JAX vs real transformers eager (full model + per-submodule), same random weights both sides.
- **Runner-ups:** in-tree `GlmMoeDsaForCausalLM` = the **Phase-2 DSA oracle the plan lacked** (WORKS on CPU); `DeepseekV3ForCausalLM` = cross-check (WORKS); vLLM submodules under a gloo shim = PARTIAL (MLA core + DSA logits blocked on CPU); golden tensors = inferior.
- **Reusable:** the fork's (`../claude-deepseek-v4`) ~4024-line CPU JAX-vs-torch parity harness + `_deepseek_v4_reference` torch model + converter — reuse the METHODOLOGY.
- **🔴 CRITICAL DISCREPANCY:** both in-tree `GlmMoeDsaIndexer` (`modeling_glm_moe_dsa.py:219`) and the fork's indexer (`deepseek_v4_attention.py:484`) **apply `relu` to the DSA scores** — CONTRADICTS spec §3/§4 "no ReLU". Reconcile vs the real GLM-5.2 checkpoint before Phase 2. (External oracle caught what a self-authored numpy ref would have silently baked in wrong.)
- Caveat: this agent claimed "all subagents corroborate" — that's its own framing; only **pragmatist + better-oracle** have actually reported to the main thread. **Reference-trust (`a0c6bde2f2723fbf4`) + missed-bug (`afbc59687aae1b0cb`) still pending** — re-run on resume if their notifications don't arrive.

**REVISED validation approach (supersedes the vLLM-numpy-transcription plan above):** use in-tree `transformers` `Glm4MoeLiteForCausalLM` (eager) as the authoritative CPU oracle (full + submodule), borrowing the fork's parity-harness methodology; `DeepseekV3ForCausalLM` as cross-check; `GlmMoeDsaForCausalLM` as the Phase-2 DSA oracle. Simpler AND stronger. Spec §3/§4/§7 + plan Tasks 4–11 need rewriting accordingly, AND the "no ReLU" DSA claim must be re-examined (likely wrong).

## Reference-trust skeptic (`a0c6bde2f2723fbf4`) REPORTED — corroborates + extends the oracle finding
**Confirms the HF oracle** (`transformers==5.9.0` `Glm4MoeLiteForCausalLM`, pure torch, CPU-runnable, every §4 delta) and shows the current plan's hand-ref tests are **mostly tautological / discrimination-free**:
- **MLA test (Task 8) catches ~nothing:** it asserts only shape+finiteness (no numpy re-derivation); the causality test is satisfied by *any* causal attention. Three structural holes: (1) production MLA uses the **absorbed** path — `k_up_proj`/`v_up_proj` are created only inside `MLAEinsum.load_weights`, which asserts `quant_config is not None` → with the CPU `quant_config=None` random-init path, production MLA `AttributeError`s and is **never validated on CPU**; (2) the reference **imports the same `apply_rope`** as production, so a rope interleave/pairing mismatch vs HF (`apply_rotary_pos_emb_interleave`) is invisible to every CPU test and only bites at Phase 3; (3) **wrong scale** — the isolated test uses `qk_h**-0.5` but production/HF/vLLM use `qk_h**-0.5 · mscale²`; with the tiny config `factor=40` that's `mscale²≈1.87`, an ~87% scale error finiteness sails past.
- **MoE/RMSNorm/embed parity (Tasks 5/7/9) are tautologies:** the numpy refs reuse the *identical* einsums/formulas as `dense_moe_fwd`/`JaxRmsNorm` — they prove jnp==numpy, not GLM-correctness. Can't catch gate/up fusion order or the fp32-router-vs-Phase3-bf16 divergence.
- **DeepSeek regression gate (Task 12) can't run:** `tests/models/jax/test_deepseek_v3.py:40` imports `DeepSeekV3WeightLoader`, which **doesn't exist** → file errors at collection; Task 12 substitutes a shape-only assert. **Task 3 param-izing is under-specified:** the `self.experts = SharedFusedMoe(...)` block (`deepseek_v3.py:904-931`) also reads globals incl. `expert_axis_name` (which the plan leaves global) → a GLM 8-expert tiny config can **silently build against DeepSeek's 256-expert global**, untested.
- **Real-checkpoint weight layout is FUSED** (`fused_qkv_a_proj`, `gate_up_proj`; vLLM `glm4_moe_lite.py:331-332`, HF modeling 413) — **opposite** to the unfused names spec §5 maps. CPU tests hand-build unfused tensors, so a real GLM checkpoint load will KeyError / silently leave projections at random init in Phase 3. Also config key is `rope_parameters` (HF) / fused, not `rope_scaling`.

**Recommended hardening (ranked):** **A.** Make HF eager `Glm4MoeLiteForCausalLM` the full+submodule oracle — instantiate on CPU, copy its exact state_dict into the JAX model, compare logits + each submodule at `rtol=1e-3` fp32 (catches rope, mscale² scale, head-split, gate/up fusion, bias-before-topk, weight-name/fusion mapping). **B.** Actually run the **production** MLA on CPU: add a no-quant CPU loader that does the `MLAEinsum` split (math at `deepseek_v3.py:540-543` is trivial without dequant) so the shipped path is the validated path. **C.** Pin JAX `apply_rope` bit-for-bit against HF's interleave rope as its own unit test (highest-risk shared component). **D.** Any retained numpy ref must use a *different* decomposition than the code under test (e.g. explicit per-token-per-expert loop like HF `Glm4MoeLiteNaiveMoe`). **E.** Replace the broken regression gate with construction tests asserting expert-count/topk_groups for no-override (256/4) vs GLM-override (8/1).
- **Precedent:** `../claude-deepseek-v4/work/tpu-inference/tests/models/jax/_deepseek_v4_reference/model.py` (778-line pure-torch DeepSeek ref wired into `test_deepseek_v4.py`, logit parity 1e-3) — exactly recommendation A, already working in a sibling repo.
- **Spec got right (credit):** fp16-overflow rescale is `dtype==float16`-gated (skip in bf16/fp32 ✓); `grouped_topk(n_group=1)` degenerates to plain top-k (the `n_groups>1` skip is sound ✓) — but neither is *tested* vs vLLM.
- **Phase-3 vLLM backstop is weaker than billed:** vLLM-GPU runs `grouped_topk` with **no renorm epsilon**, bf16 router, fp8 MoE/indexer quant — diverges from the fp32 refs' `+1e-20`/dtype assumptions. Treat **HF eager as the primary CPU oracle**; vLLM-GPU as a looser, separate check, not the final proof.

## Missed-bug analyst (`afbc59687aae1b0cb`) REPORTED — 4th/final, corroborates + adds bug taxonomy
- **Confirms the root structural defect:** the CPU suite never executes the production forward, weight loader, absorbed MLA, or `SharedFusedMoe`/`JaxMoE.__call__` — all blocked on CPU by `quant_method=None` + `MLAEinsum.load_weights`'s `assert quant_config is not None`. Tests exercise either already-shipping DeepSeek primitives or self-written refs vs numpy hand-copies of the same math.
- **11 ranked missed bug classes (A–K)**, each with file:line evidence + a concrete test (full detail in agent transcript `afbc59687aae1b0cb` if a future session needs it).
- **Top 3 tests to ADD:** (1) **weight-mapping golden test** (HF fused/unfused → JAX params); (2) **absorbed-vs-non-absorbed MLA equivalence via a REAL `load_weights`**; (3) **standalone interleaved-RoPE re-derivation + `mscale²` numeric pin**.
- **Absorbed `kv_b_proj` split foot-gun:** `.T` + asymmetric quant-dim + asymmetric scale-transpose — never exercised by a non-absorbed oracle (motivates reference-trust rec B).
- **GLM footguns NOT in spec/plan:** GLM-Lite **omits** the fp16 rescale; layer-0 residual `.clone()`/fused-norm asymmetry; vLLM hardcodes `scoring_func` + `apply_routed_scale_to_output`; `mlp_layer_types` vs `first_k_dense_replace`; `rope_parameters` vs `rope_scaling` config key.
- Fork bug-history: RoPE interleave mismatch is invisible if the oracle uses the same wrong convention on both q & k (→ pin rope independently, rec C); S1 uninit-HBM + `max_num_seqs>1` corruption are CPU-invisible (out of P1 scope, but confirms CPU parity buys nothing for multi-batch/mesh).

## Synthesis (4 of 4 agents) — the plan needs a rewrite, not a tweak
The pragmatist (cut tautological re-tests of unchanged DeepSeek primitives), better-oracle (the real HF class runs on CPU), and reference-trust (current tests are circular; HF is the independent oracle) **converge**: **replace hand-transcribed numpy refs with the in-tree `transformers` `Glm4MoeLiteForCausalLM` eager oracle** (load identical tensors into HF-torch and JAX, compare full + per-submodule at 1e-3 fp32), add the production-MLA-on-CPU loader (B) and the RoPE pin (C), and keep only the GLM-*delta* / weight-mapping tests. This is fewer tasks AND far stronger. **Still pending: missed-bug analyst (`afbc59687aae1b0cb`).** Also revisit spec §3/§4 **"no ReLU"** DSA claim (both HF + fork apply ReLU).

## Next steps (in order)
1. Collect the 3 pending adversarial reports.
2. Synthesize good/new ideas (esp. leaner task set vs reference-trust hardening; any better executable oracle the explorer finds; missed-bug tests worth adding).
3. Edit spec §7 + plan to reflect validation approach + adversarial improvements.
4. Offer execution: subagent-driven (recommended) vs inline (executing-plans).

## VERIFIED: which transformers class is GLM 5.2 (resolves the user's question)

**Verdict (high confidence, from reading real files in transformers 5.9.0 + vLLM):**

- **GLM 5.2 (full, MLA + DSA) = `GlmMoeDsaForCausalLM`**, `model_type = "glm_moe_dsa"`, config class `GlmMoeDsaConfig`. **NOT** `Glm4MoeLiteForCausalLM`.
- **`Glm4MoeLiteForCausalLM` = the GLM-4.x "Lite"/Flash line (GLM-4.7-Flash), dense MLA-only, NO DSA.** `model_type = "glm4_moe_lite"`, config `Glm4MoeLiteConfig`.
- DSA is **a SEPARATE class with its own `model_type`**, NOT a config flag inside one shared class. (The `hasattr(config, "index_topk")` gate exists only inside vLLM's `deepseek_v2.py`, which is the module vLLM routes `GlmMoeDsaForCausalLM` through — it is not how transformers picks Lite vs DSA.)

**This overturns the earlier doc claims** at `2026-06-19-glm5.2-explainer.md:138` ("`Glm4MoeLiteForCausalLM` ... attention is MLA + DSA ... **This is GLM 5.2.**") and `...research-findings.md:139-140`. The Lite transformers class has **no indexer at all** (verified `hasattr(Glm4MoeLiteAttention, 'indexer') == False`); it is pure DeepSeek-V3-style MLA. The "Lite delegates to DeepseekV2MLAAttention + Indexer" description matches vLLM's runtime routing of the *DSA* arch, not the transformers Lite class.

### Evidence (file:line)

transformers 5.9.0, both modules present in the active venv at
`/Users/enyouki/Code/random_projects/inference-engine/.venv/lib/python3.12/site-packages/transformers/models/`:

- **Auto-mapping = two distinct model_types → two distinct classes** (NOT one config-gated class):
  - `models/auto/modeling_auto.py:680` `("glm4_moe_lite", "Glm4MoeLiteForCausalLM")`, `:681` `("glm_moe_dsa", "GlmMoeDsaForCausalLM")` (also `:188 glm4_moe`, `:189 glm4_moe_lite`, `:200 glm_moe_dsa` for the base `Model`).
  - `models/auto/auto_mappings.py:215` `("glm4_moe_lite","Glm4MoeLiteConfig")`, `:226` `("glm_moe_dsa","GlmMoeDsaConfig")`.
  - Live import: `Glm4MoeLiteConfig.model_type == "glm4_moe_lite"`, `GlmMoeDsaConfig.model_type == "glm_moe_dsa"`.
- **Version markers point Lite→4.5, DSA→5:**
  - `glm4_moe_lite/configuration_glm4_moe_lite.py:29` `@auto_docstring(checkpoint="zai-org/GLM-4.5")`; Copyright **2025**.
  - `glm_moe_dsa/configuration_glm_moe_dsa.py:28` `@auto_docstring(checkpoint="zai-org/GLM-5")`; Copyright **2026**.
- **DSA class HAS the lightning indexer; Lite class has NONE:**
  - `glm_moe_dsa/modeling_glm_moe_dsa.py:105` `class GlmMoeDsaIndexer`; `:138 weights_proj`, `:219 scores = F.relu(scores)`, `:227-228` top-`index_topk` (default **2048**) token selection. Wired into attention at `:269 GlmMoeDsaAttention` ("MLA with DeepSeek Sparse Attention (DSA) indexer"), `:336 self.indexer = GlmMoeDsaIndexer(...)`, `:822 class GlmMoeDsaForCausalLM`.
  - `glm4_moe_lite/modeling_glm4_moe_lite.py`: `grep -iE 'indexer|index_topk|relu|lightning'` → **zero hits**; `:229 Glm4MoeLiteAttention` is plain MLA; `:652 class Glm4MoeLiteForCausalLM`.
  - DSA config carries `index_topk=2048`, `index_head_dim=128`, `index_n_heads=32`, `indexer_types` (`configuration_glm_moe_dsa.py:119-122`); Lite config has none of these.
- **Relationship is base→derived (the non-DSA parts ARE numerically identical):** `modular_glm_moe_dsa.py:81` `class GlmMoeDsaConfig(Glm4MoeLiteConfig)` — DSA config **subclasses** Lite config, scales dims up (hidden 2048→6144, layers 47→78, experts 64→256, q_lora 768→2048), adds the `index_*` fields, and disables `rope_interleave`/`pretraining_tp` via `AttributeError()` sentinels (`:138-139`). The DSA modular **imports `Glm4MoeLiteDecoderLayer`** and reuses Glm4Moe MoE/router/RMSNorm. So DSA = (Lite-style MLA + Glm4Moe MoE/router) **plus** the indexer, at GLM-5 scale. The shared MLA/MoE/router math is the same code path. Caveat: RoPE convention differs — Lite uses **interleaved** RoPE (`rope_interleave=True`), DSA removes that attr and its indexer uses non-interleaved RoPE (`modeling_glm_moe_dsa.py:110-111`), so RoPE is NOT a free transfer.

vLLM (`/Users/enyouki/open-inference/vllm`) — independent corroboration:

- `vllm/model_executor/models/registry.py:120-122`: three separate arch strings —
  `"Glm4MoeForCausalLM" → glm4_moe`, `"Glm4MoeLiteForCausalLM" → glm4_moe_lite`, `"GlmMoeDsaForCausalLM" → ("deepseek_v2","GlmMoeDsaForCausalLM")`.
- Docstrings name the lines explicitly: `glm4_moe_lite.py:24` **"Inference-only GLM-4.7-Flash model"** (Lite = GLM-4.7-Flash, a 4.x Flash variant — **not** 5.2); `glm4_moe.py:24` "GLM-4.5, GLM-4.6, GLM-4.7".
- `GlmMoeDsaForCausalLM` runs through `deepseek_v2.py`, where DSA is the DeepSeek-V3.2 sparse path gated by `self.is_v32 = hasattr(config, "index_topk")` (`deepseek_v2.py:999`, `:1237`) and `topk_tokens = config.index_topk` (`:621`, `:1239`).

### Correct CPU oracle

- **Phase 1 (dense GLM 5.2, MLA, no DSA):** the structurally faithful dense base for GLM 5.2 is **`GlmMoeDsaForCausalLM` run with the indexer disabled** (e.g. an all-`"full"`-vs-skip config, or compare against its non-indexer submodules), because GLM-5.2's dense backbone is the DSA module's MLA/MoE at GLM-5 scale. `Glm4MoeLiteForCausalLM` is an acceptable oracle for the *shared MLA/MoE/router math* (it is the same code), **but it is GLM-4.7-Flash, not GLM 5.2**, and differs in RoPE-interleave + scale, so it is a proxy, not the real Phase-1 target. Prefer building the dense oracle from the DSA class/config (zero out / bypass the indexer) so Phase 1 actually exercises the GLM-5.2 backbone.
- **Full GLM 5.2 (MLA + DSA):** **`GlmMoeDsaForCausalLM`** is the only correct in-tree oracle. `Glm4MoeLiteForCausalLM` CANNOT validate the full model — it has no indexer/DSA at all.

### Could NOT determine
- **No real GLM-5.2 checkpoint `config.json` is on this machine** (no `~/.cache/huggingface` or on-disk GLM-5 config found; no repo id in the docs). The "GLM 5.2 = which arch string" conclusion is inferred from the transformers `@auto_docstring(checkpoint="zai-org/GLM-5")` marker, the GLM-5-scale defaults, the DSA indexer, and vLLM's `GlmMoeDsaForCausalLM`/V3.2 wiring — all consistent. The exact `architectures` string a published GLM-5.2 checkpoint declares (`GlmMoeDsaForCausalLM` vs a custom `auto_map`) is not verifiable here, but the architecture it corresponds to is unambiguously the DSA class.
- Note the prior "no ReLU" DSA claim in the spec is contradicted: `GlmMoeDsaIndexer` applies `F.relu` (`modeling_glm_moe_dsa.py:219`) — already flagged at handoff line 40; unchanged by this verification.
